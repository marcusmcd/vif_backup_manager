#ifndef UTIL_H
#define UTIL_H
/*! \fn void date_string(char* string_loc)
    \brief Opens a file descriptor.
    \param pathname The name of the descriptor.
    \param string_loc Opening flags.
*/
void date_string(char* string_loc);

#endif
